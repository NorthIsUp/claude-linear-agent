apiVersion: v1
kind: Service
metadata:
  name: {{ include "linear-claude-bridge.fullname" . }}
  labels:
    {{- include "linear-claude-bridge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "linear-claude-bridge.selectorLabels" . | nindent 4 }}
